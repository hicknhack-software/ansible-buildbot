---

- name: enable buildbot master
  service:
    name: buildbot
    enabled: yes

- name: restart buildbot master
  service:
    name: buildbot
    state: restarted
  when: buildbot_master_restart | default(False)
